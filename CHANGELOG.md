# Changelog

Dated log of code changes made to this project (see CLAUDE.md's "Changelog" instruction). Newest entries at the top.

## 2026-09-25

- **Shelf Map layout editing re-opened to Store Managers** (reverses the view+count-only change from 2026-09-24, per "can we let the store manager access edit the shelf"): `admin_save_shelf_map`/`admin_delete_shelf_map` in [supabase_shelf_maps.sql](sql/supabase_shelf_maps.sql) use `is_shelf_map_editor_authorized` (Super User OR Store Manager) again, and [shelfMap.js](docs/js/shelfMap.js) shows Edit Layout/New Shelf to both. Re-run the SQL.

- **Custom aquarium safety rule: 4 ft (48 in) tall or more requires 19mm (3/4") glass** (threshold is `>= 48`, corrected from `> 48` at the user's request): new first check in `validateGlassSafety` ([custom-aquarium-calculator.js](docs/WebAquariumCalculator/custom-aquarium-calculator.js)) auto-upgrades the glass straight to 19mm (priced at the 2000/sq ft 19mm rate). "19mm (3/4")" added to the aquarium glass dropdowns in [index.html](docs/WebAquariumCalculator/index.html), [order-now.html](docs/order-now.html) and [gma-conversations.html](docs/gma-conversations.html); Order Now's tier walk in [orderNow.js](docs/js/orderNow.js) now ends at 19mm. Mirrored in [chatbot-engine.ts](supabase/functions/_shared/chatbot-engine.ts) (validator, message helper, `glass_thickness` enum/description) so Alice quotes the same - not deployed yet.


- **New one-off [supabase_item_ledger_backfill_transfer.sql](sql/supabase_item_ledger_backfill_transfer.sql)**: writes the missing Item Ledger entries (Transfer Shipment / Transfer Receipt) for transfer order(s) shipped while "Transfer posting" was switched off. Edit the order number(s) at the top; posts only the difference vs. what the ledger already holds, so it's safe to re-run, and doesn't refuse when the source is short (lists any negative stock after).

## 2026-09-25

- **Transfer Orders: new "Create PO" button** in the Manage modal ([transfer-orders.html](docs/transfer-orders.html), [transferOrders.js](docs/js/transferOrders.js)). Finds every line where the From Warehouse's on-hand (Item Ledger, same as the Available column) is less than what's still to ship, groups the missing quantities by each item's primary vendor, and creates one Purchase Order per vendor (received into the From Warehouse, note "Created from Transfer Order X") via the existing `staff_create_purchase_order`. Quantities are editable and a vendor can be unticked before creating; items with no Vendor No. are listed and skipped. Hidden for Store Managers (no access to PO pages). New [supabase_transfer_order_create_po.sql](sql/supabase_transfer_order_create_po.sql) (`staff_get_items_vendor_info`) - needs running. The AI bot is unaffected.

## 2026-09-24

- **Shelf Map shows the last replenishment transfer order** for the selected shelf ([shelf-map.html](docs/shelf-map.html) / [shelfMap.js](docs/js/shelfMap.js) `loadLastReplenishment`): next to the location label, "Last replenishment TO: <no.> - status, requested <date> by <user>", linking to that order (Transfer Orders if still open, otherwise Posted Transfer Orders). Found by the "Shelf replenishment - <shelf name>" description the button writes; refreshes after creating one. No SQL change.

- **New Defect Items module (report -> approve -> stock out)**: [defect-items.html](docs/defect-items.html) / [defectItems.js](docs/js/defectItems.js) under Inventory (Store Managers + Super Users, added to `nav.js` and the store-manager page allow-list in `auth.js`). A store manager reports an item (own location only) with a reason, note and a **required photo** (compressed in the browser, uploaded through a signed URL to the new `defect-photos` bucket); it stays Pending and moves no stock. A super user approves (posts a Negative Adjmt. to the Item Ledger, document type "Defect", doc no `DEF-000001`, refused if not enough on hand) or rejects with a reason; the reporter can withdraw while Pending. Needs [supabase_defect_reports.sql](sql/supabase_defect_reports.sql) (table, RPCs, bucket). Item Ledger Entries' Show Document now opens the report for Defect entries. AI bot unaffected.

- **Transfer Orders: a manually typed Qty To Ship now sticks** ([transferOrders.js](docs/js/transferOrders.js)). The stock auto-fill was re-running on every open and overwriting hand-typed quantities (and could overwrite a box while it was being typed in). Now: boxes already being edited are skipped; a manual change is saved with a new `Transfer_Line."Qty To Ship Manual"` flag ([supabase_transfer_line_qty_to_ship_manual.sql](sql/supabase_transfer_line_qty_to_ship_manual.sql) - run it) and kept on later opens; Ship clears the flag so the next round fills from stock again. Manual edits also save when Item Ledger posting is off.

- **Qty To Ship auto-fill now shows even when the viewer can't ship** ([transferOrders.js](docs/js/transferOrders.js)): the box is set to min(unshipped, available) - so 0 when nothing is available - for anyone who sees it; only the save (and the save-on-edit) stays limited to someone who can ship. Before, it did nothing at all when the Ship button was hidden.

- **Transfer Orders: Qty To Ship auto-fills from available stock and auto-saves** ([transferOrders.js](docs/js/transferOrders.js) `autoFillQtyToShip`): when someone who can ship opens the order, each line's Qty To Ship = the lesser of the unshipped balance and the From Warehouse's available qty (never below 0), saved to `Transfer_Line."Qty To Ship"` right away; a hand-typed value is saved on change. Skipped when Transfer Orders aren't posting to the Item Ledger. Serial-tracked lines' required-serial count follows the new qty. No SQL change.

- **Transfer Order "Print (Standard Layout)" now shows Qty To Ship** ([transfer-order-print.html](docs/transfer-order-print.html) / [transferOrderPrint.js](docs/js/transferOrderPrint.js)): new column between Qty To Transfer and Qty Shipped - the saved Qty To Ship, or the unshipped balance when none was entered. The Production layout is unchanged.

- **New one-off [supabase_shelf_map_fish_food_accessories_amaya.sql](sql/supabase_shelf_map_fish_food_accessories_amaya.sql)**: creates the "Fish Food & Accessories" shelf (24 cells over 6 rows) from the second hand-drawn sketch. Name and location (Amaya) are assumptions set at the top of the script. Labels/sizes/drawn qty only - not linked to Items yet.

- **Transfer Orders: super-user "Delete Order"** ([transfer-orders.html](docs/transfer-orders.html) / [transferOrders.js](docs/js/transferOrders.js)): deletes the Transfer_Header and all its Transfer_Lines, only when no line has any Qty Shipped/Received (re-checked from the database right before deleting, since shipping writes Item Ledger entries a delete wouldn't undo). Works for any status incl. Cancelled. No SQL change.

- **Replenishment TO: single-variant items are no longer skipped** ([shelfMap.js](docs/js/shelfMap.js)): nearly every item has exactly one variant, which made the button refuse everything ("Every item ... has variants"). Now an item with one variant uses it on the line; only items with several variants are left off and listed.

- **Shelf Map: "Create Replenishment TO" button** ([shelf-map.html](docs/shelf-map.html) / [shelfMap.js](docs/js/shelfMap.js)): creates a Requested transfer order from the "Warehouse" location to the shelf's location, one line per linked item at half of its shelf qty or less (the orange ones), qty = shelf qty - ledger on-hand. Same Transfer_Header/Transfer_Line writes as New Transfer Order; shows a confirm with the lines first, then offers to open it. Items with variants are left off and listed (pick the variant on the Transfer Orders page). Estimated Delivery Date is not set. No SQL change; AI bot unaffected.

- **Shelf Map colours now mean "restock" instead of "count differs"** ([shelfMap.js](docs/js/shelfMap.js), [shelf-map.html](docs/shelf-map.html)): orange = ledger on-hand is half of the shelf qty or less, green = more than half, red (out of stock) and grey (unlinked) unchanged.

- **New Glass thickness "19mm (3/4")" at 2000/sq ft** on the Sticker calculator: Glass type now offers 19mm ([custom-aquarium-calculator.js](docs/WebAquariumCalculator/custom-aquarium-calculator.js) `getStickerThicknessOptions`/`DEFAULT_GLASS_PRICES`, label in [sticker.html](docs/WebAquariumCalculator/sticker.html); Order Now and GMA pick it up automatically as plain "19mm"). Mirrored in [chatbot-engine.ts](supabase/functions/_shared/chatbot-engine.ts) (default price + `thickness` enum) so Alice quotes it - not deployed yet. Optional [supabase_glass_pricing_19mm.sql](sql/supabase_glass_pricing_19mm.sql) adds the row to Pricing Setup so it's editable there. Aquariums do not offer 19mm.

- **Sticker calculator labels**: dropped the price captions - "Tempered (2x price)" -> "Tempered", "Repair (2.5x price)" -> "Repair" in [sticker.html](docs/WebAquariumCalculator/sticker.html). Pricing unchanged.

- **Sticker calculator: new "Tempered" checkbox for Glass** ([sticker.html](docs/WebAquariumCalculator/sticker.html)). Tempered = 2x the glass rate (same 2x the custom aquarium calculator uses), stacks with Repair (2.5x). Added to the shared `calculateStandaloneSticker` (`tempered` option) in [custom-aquarium-calculator.js](docs/WebAquariumCalculator/custom-aquarium-calculator.js) and its hand-ported copy in [chatbot-engine.ts](supabase/functions/_shared/chatbot-engine.ts) (+ `is_tempered` on the `compute_sticker_quote` tool schema) so Alice quotes the same. Bot edge functions not deployed yet. Order Now's and GMA's sticker forms don't have the checkbox yet.

- **Fix the Amaya shelf script's `RAISE` error** ("too few parameters specified for RAISE"): the literal `%` in `"Amaya%"` in [supabase_shelf_map_infinity_double_drawer_amaya.sql](sql/supabase_shelf_map_infinity_double_drawer_amaya.sql) is now escaped as `%%`.

- **New one-off [supabase_shelf_map_infinity_double_drawer_amaya.sql](sql/supabase_shelf_map_infinity_double_drawer_amaya.sql)**: creates the "Infinity Plastic Double Drawer Filter Box" shelf for Amaya (60 cells over 14 rows) from the hand-drawn sketch. Labels/model codes/drawn qty only - cells aren't linked to Items yet.

- **Show Document now picks the open or posted page**: [itemLedgerEntries.js](docs/js/itemLedgerEntries.js) checks whether the PO / transfer order still exists (`staff_get_purchase_order` / `Transfer_Header`) and opens purchase-orders / transfer-orders if so, otherwise posted-purchase-orders / posted-transfer-orders. Posted transfers deep-link to the document; posted POs are filtered to it via `?search=`.

- **Removed the Receive button from the Purchase Order card** ([purchase-orders.html](docs/purchase-orders.html) / [purchaseOrders.js](docs/js/purchaseOrders.js)) - Post Purchase Order now does the receiving (partial receipts stay open). Deleted the unused `receivePurchaseOrderQuantities`.

- **Post Purchase Order now also receives the stock** ([purchaseOrders.js](docs/js/purchaseOrders.js), no SQL change): Post receives the quantities in the Qty Received boxes (prefilled with what's outstanding) through `staff_receive_purchase_order_lines`, then archives the PO only if nothing is left outstanding. A partial receipt is received but the PO is NOT posted (stays "Partially Received"), with a prompt saying so. The Receive button still works. AI bot unaffected (reads `QuantityInStock`, which follows the ledger).

- **Item Ledger Entries: "Posted By" column + "Show Document" action** (BC-style) in [item-ledger-entries.html](docs/item-ledger-entries.html) / [itemLedgerEntries.js](docs/js/itemLedgerEntries.js). Show Document opens the source doc in a new tab: Sales Order -> online-order-lines, Purchase Receipt -> purchase-orders, Transfer Shipment/Receipt -> transfer-orders (reversals map to the original's document); disabled for adjustments/stock counts/opening balances. purchase-orders.js and postedPurchaseOrders.js now accept `?search=`. No SQL change.

- **New one-off [supabase_item_ledger_delete_0813pm_seed.sql](sql/supabase_item_ledger_delete_0813pm_seed.sql)**: deletes the Item Ledger entries posted 2026-09-24 8:13 PM (Manila, the Pancake opening-balance seed) so the beginning balance can be reloaded; also turns sales posting back off and clears `ItemLedgerOrderSync`. Read-only preview step first.

- **New one-off [supabase_item_ledger_wipe_except_0520pm.sql](sql/supabase_item_ledger_wipe_except_0520pm.sql)**: deletes every Item Ledger entry except those posted 2026-09-24 5:20 PM (Manila) so the beginning balance can be reloaded; also turns sales posting back off and clears `ItemLedgerOrderSync`. Has a read-only preview step first.

- **Item Ledger Entries grid now shows the time posted**: new "Posted At" column (time of day, from `posted_at_utc`, already returned by the list RPC) next to Posting Date in [item-ledger-entries.html](docs/item-ledger-entries.html) / [itemLedgerEntries.js](docs/js/itemLedgerEntries.js). No SQL change.

- **Fix "Could not start: UPDATE requires a WHERE clause"** on "Start Posting Sales From Now": Supabase rejects an `UPDATE` with no `WHERE`, so the `ItemLedgerSetup` update in `admin_start_item_ledger_sales_posting` ([supabase_item_ledger_stock_count.sql](sql/supabase_item_ledger_stock_count.sql)) and the seed script now use `where "Id"`.

- **Shelf Map: Store Managers are view + count only** (layout editing stays Super User only). Reverted the brief Store Manager layout-edit access: `admin_save_shelf_map`/`admin_delete_shelf_map` in [supabase_shelf_maps.sql](sql/supabase_shelf_maps.sql) use `is_admin_authorized` again (the script also drops the short-lived `is_shelf_map_editor_authorized`), and [shelfMap.js](docs/js/shelfMap.js) shows Edit Layout/New Shelf to Super Users only. Re-run the SQL.

- **Shelf Map is now per location, with a live count per shelf spot**: new Location dropdown (shelves grouped by warehouse; a shelf must have a location to be saved), and each spot has an editable "current count" (type it right on the map, saved via new `staff_set_shelf_cell_count`, records who/when). Colour now compares the shelf counts (summed across all spots of that location holding the item) against the ledger. [supabase_shelf_maps.sql](sql/supabase_shelf_maps.sql) gained `CurrentQty/CountedAtUtc/CountedBy` columns (existing drawing counts are copied over once) - re-run it. [shelfMap.js](docs/js/shelfMap.js), [shelf-map.html](docs/shelf-map.html).

- **New Shelf Map page** ([shelf-map.html](docs/shelf-map.html), [shelfMap.js](docs/js/shelfMap.js), Inventory menu): draw a shelf as rows of spots, link each spot to an item, and see that item's live Item Ledger quantity at the shelf's warehouse next to the count written on the paper drawing (green = matches, amber = differs, red = out of stock). Super users edit the layout (add/move/remove spots, warehouse, "Auto-link items" by model code); everyone else views. New [supabase_shelf_maps.sql](sql/supabase_shelf_maps.sql) (`ShelfMaps`, `ShelfMapCells`, `staff_get_shelf_maps`, `admin_save_shelf_map`, `admin_delete_shelf_map`) also seeds the first drawing ("Shelf 1 - Pumps & Filters"). Needs the SQL run. Store Managers can view it ([auth.js](docs/js/auth.js) allowlist, [nav.js](docs/js/nav.js)). The AI bot is unaffected (no customer-facing change).

- **Bot no longer volunteers "wala kaming stock" on plain price questions**: in `buildSystemPrompt` ([chatbot-engine.ts](supabase/functions/_shared/chatbot-engine.ts)) the stock rule now says an item that exists with 0 stock still gets just its price (only truly-missing items or explicit availability questions mention stock), and the "50 gallon" standard-tank rule no longer says "in stock". Needs the `chatbot-*` edge functions redeployed.

- **Dashboard site-wide "Monthly Sales Target" card now super-user only**: [dashboard.js](docs/js/dashboard.js) passes a 0 target to `updateSalesTargetTile` for non-super users, which hides the card (per-staff target bars on the Sales by Staff cards are unchanged).
- **Online orders stuck on Printed/Confirmed/To Ship after Pancake marked them Delivered**: new [supabase_online_order_open_status_refresh.sql](sql/supabase_online_order_open_status_refresh.sql) adds `cron_refresh_open_online_order_statuses` (every minute, 40 orders/run, least-recently-refreshed first) that re-fetches each still-open order's detail from Pancake and overwrites `Status`, independent of the `updated_after` cursor that was skipping them. Needs to be run in the Supabase SQL editor.

- **Alice can now actually look at photos customers send on Messenger** (previously the image was only run through a payment-screenshot check, then a canned "someone will follow up" reply). [facebook-messenger-webhook/index.ts](supabase/functions/facebook-messenger-webhook/index.ts) now attaches the current photo to the customer's message in the main AI turn (captioned or not); only payment screenshots (or a photo that couldn't be loaded) still take the old canned/staff-handoff path. New photos rule in [chatbot-engine.ts](supabase/functions/_shared/chatbot-engine.ts): describe/help from the photo, cautious non-diagnostic fish-health guidance, never quote prices/sizes from a photo alone, damaged-item photos escalate. Website widget (`chatbot-web-reply`) has no photo upload at all, so it isn't covered. Deployed all five consumers of `chatbot-engine.ts` (`facebook-messenger-webhook`, `chatbot-web-reply`, `portal-chat-alice-reply`, `telegram-alice-webhook`, `chatbot-sandbox-reply`).

## 2026-09-23

- **Physical Inventory Journal is now a blind count for non-Super Users**: Qty. (Calculated) and Quantity (the variance, which would reveal it) are hidden from the grid, the line detail panel (also Qty. (Current)) and the printed count sheet unless `session.isSuperUser`. [physicalInventoryJournal.js](docs/js/physicalInventoryJournal.js) + a `.calc-only` CSS rule in [physical-inventory-journal.html](docs/physical-inventory-journal.html). UI-level only - the RPCs still return the figures.

- **Item Setup item card now shows "Stock by Location"** (actual on-hand per warehouse from the Item Ledger, BC-style). New [supabase_item_setup_stock_by_warehouse.sql](sql/supabase_item_setup_stock_by_warehouse.sql) (`staff_get_item_stock_by_warehouse`), plus a new row in [item-setup.html](docs/item-setup.html) filled by `loadItemCardStockByLocation` in [itemSetup.js](docs/js/itemSetup.js). Each quantity is a link that opens [item-ledger-entries.html](docs/item-ledger-entries.html) filtered to that item + location (all time); [itemLedgerEntries.js](docs/js/itemLedgerEntries.js) gained a `?warehouse=` deep link to go with the existing `?search=`.

- **Alice now reports stock per branch (Amaya vs GMA)**, like Business Central's per-location inventory. New [supabase_chatbot_stock_by_location.sql](sql/supabase_chatbot_stock_by_location.sql) adds a `stock_by_location` jsonb column (from `ItemLedgerEntries` per warehouse, matched by warehouse name) to `public_search_items` and `public_list_order_items`; [chatbot-engine.ts](supabase/functions/_shared/chatbot-engine.ts) tool descriptions + stock rule updated to use it. Needs the SQL run, then the bot functions redeployed.

- **Alice now knows the branch pin locations** ("RSPetStop Amaya" and "RSPetStop GMA") and tells customers to search that exact name in Google Maps/Waze when they ask where the branches are. New BRANCH PIN LOCATIONS block in `buildSystemPrompt` in [chatbot-engine.ts](supabase/functions/_shared/chatbot-engine.ts). Not deployed yet.

- **Alice no longer volunteers stock count/availability on a plain price question**, per a Messenger chat where a "what's the price of a 20 GAL tank" got both the price AND an unprompted "It's currently showing 0 in stock" plus a wholesale upsell in the same reply. [chatbot-engine.ts](supabase/functions/_shared/chatbot-engine.ts)'s WHAT YOU CAN HELP WITH rule now only has her mention stock/quantity when the customer actually asks about availability, or the item isn't in the catalog at all - same "don't volunteer" pattern already used for wholesale pricing and photos.

- Deployed `facebook-messenger-webhook`, `chatbot-web-reply`, `portal-chat-alice-reply`, `telegram-alice-webhook`, and `chatbot-sandbox-reply` (all consumers of the shared `chatbot-engine.ts`) twice today so the stock-volunteering fix, the geocoding key-suffix diagnostic, and the two Lalamove geocoding fixes below all reach production, per direct request each time.

- **Added a barangay/city-level fallback to Alice's Lalamove address geocoding**, per a follow-up report that "blk 6 lot 7 brgy luzviminda 2 dasmarinas cavite" still failed even after the REQUEST_DENIED-vs-zero-result fix below - while the store's own Places Autocomplete widget (Google Maps address box, `GOOGLE_MAPS_API_KEY`) resolves the shorter "brgy luzviminda 2 dasmarinas" to "Brgy Luzviminda II Hall" without issue, since the plain Geocoding API (`GOOGLE_ROUTES_API_KEY`) the bot uses is much less tolerant of a run-on informal address string than the Autocomplete/Places family the widget uses. `geocodeAddressWithFallback()` in [chatbot-engine.ts](supabase/functions/_shared/chatbot-engine.ts) now retries with just the barangay-and-up portion (or the last few words as a city/province-level guess) when the full address comes back empty, and `compute_lalamove_quote`'s result now carries `approximate: true` + a note when it had to fall back, so Alice tells the customer the fee is an area-level estimate rather than presenting it as exact. Genuinely unresolvable addresses (fails at both levels) now ask for a landmark/more complete barangay name instead of just "double check it."

- **Fixed Alice wrongly telling customers a perfectly valid delivery address "can't be found" for Lalamove quotes**, per a Messenger chat where she rejected a full, correct Blk/Lot/Brgy Dasmariñas address three times in a row. Root cause in [chatbot-engine.ts](supabase/functions/_shared/chatbot-engine.ts)'s `geocodeAddress()` (backs `compute_lalamove_quote`, since Lalamove's API needs lat/lng - unlike `compute_delivery_quote`'s store-truck formula, which hands Google Routes API a plain address string): it only checked `results[0]`, so a Google Geocoding API *service* failure (`REQUEST_DENIED`/`OVER_QUERY_LIMIT`/etc - e.g. the Geocoding API not being enabled/authorized for `GOOGLE_ROUTES_API_KEY`, as opposed to Routes API which clearly is enabled) looked identical to a genuine zero-result and got blamed on the customer's address. Now checks Google's `status` field, logs the real reason server-side, and returns a distinct "mapping service failed, don't blame the address - escalate to staff" result when it's a service/config error rather than an actual bad address; also added `region=ph` to the geocode request to bias informal local addresses correctly. **Still needs a manual check**: open Google Cloud Console → the project behind `GOOGLE_ROUTES_API_KEY` → confirm the Geocoding API is both enabled and included in that key's API restrictions (Routes API alone being enabled would explain exactly this symptom) - I can't check/change that from here.

- **Alice can now share the full wholesale price list, not just quote one item at a time.** Previously she only had `search_items` (needs a matching product keyword - "wholesale price list" itself matches nothing) and `list_items_in_category` (never selects `WholesalePrice` at all), so a customer asking for the wholesale list got told there's no wholesale price. New [supabase_chatbot_list_wholesale_items.sql](sql/supabase_chatbot_list_wholesale_items.sql) adds `public_list_wholesale_items()` (same eligibility gate as the per-item lookup: category `IsWholesaleApplicable` = true - currently Aquarium/Stand/Sump - AND an actual `WholesalePrice` set on the item); wired up as a new `list_wholesale_prices` tool + WHOLESALE PRICING prompt rule in [chatbot-engine.ts](supabase/functions/_shared/chatbot-engine.ts), shared by both the Messenger bot and the website widget.

- **Opened the Physical Inventory Journal to Store Manager accounts, not just Super User**, per "can you show this to manager permission too?" New [supabase_phys_journal_store_manager_access.sql](sql/supabase_phys_journal_store_manager_access.sql) adds a dedicated `is_phys_journal_authorized()` helper (Super User OR Store Manager, re-verified per call) and swaps it into the page's core RPCs (batches, get lines, calculate, add/edit/delete line, clear batch, post, and the two Pancake-live-stock helpers) - deliberately a separate helper rather than widening `is_admin_authorized()` itself, same reasoning as Conversations Access below. Added `physical-inventory-journal.html` to Store Manager's page allowlist ([auth.js](docs/js/auth.js)) and its nav link now also shows for Store Managers ([nav.js](docs/js/nav.js)). Known gap, flagged not fixed: "Add Line"'s item/variant search (`admin_list_items`/`admin_list_variants`) and the per-line availability-by-location fact box (`admin_get_item_ledger_balances`) are shared with other Super User-only pages (Item Setup, Variant Setup, Item Ledger Entries) and were left as-is - a Store Manager can still Calculate Inventory, count, and Post normally, just not manually search for a stray item to add, or see a line's per-warehouse breakdown. Say the word if that should open up too.

- **Fixed "Not authorized." inside GMA Conversations for Conversations Access accounts** - the page loaded, but every RPC it calls (conversation list, messages, orders, quick replies, media library, sending replies, etc.) independently re-checks authorization server-side via `is_admin_authorized()`, which only allows Super User and was untouched by the earlier page-level fix. New [supabase_gma_conversations_staff_rpc_access.sql](sql/supabase_gma_conversations_staff_rpc_access.sql) adds a dedicated `is_conversations_authorized()` helper (Super User OR Conversations Access, re-verified per call, same trust model as `is_admin_authorized()`) and swaps it into the ~25 RPCs the page actually uses - deliberately a separate helper rather than widening `is_admin_authorized()` itself, since that one also gates dozens of unrelated Super User-only admin pages (User Setup, Item Setup, Pricing Setup, etc.) that shouldn't be affected. Known gap, not fixed: the "Import Facebook History" button calls an Edge Function (`facebook-conversations-backfill`) that re-checks Super User in its own TypeScript, not via an RPC - it stays Super User-only unless that function is separately patched.

- **Fixed Store Manager accounts with "Conversations Access" also ticked getting bounced straight back to Dashboard when clicking GMA Conversations.** Cause: `js/auth.js`'s `requireAuth()` enforces Store Manager's page allowlist (`STORE_MANAGER_ALLOWED_PAGES`) before the page's own permission check ever runs, and `gma-conversations.html` wasn't on that list - so the exclusive lockdown redirected to `dashboard.html` regardless of the Conversations Access flag. Added `gma-conversations.html` to the allowlist; the page's own `isSuperUser || isConversationsStaff` gate still controls actual access for Store Managers who don't have Conversations Access checked. [auth.js](docs/js/auth.js)

- **Added a "Conversations Access" per-user permission for GMA Conversations**, per "can you add new permission per user to be able to assign this" - the page was previously Super User-only with no separate flag. New [supabase_staff_users_conversations_staff_field.sql](sql/supabase_staff_users_conversations_staff_field.sql) adds `StaffUsers."ConversationsStaff"`, threaded through `verify_login`/`admin_list_staff_users`/`admin_create_staff_user`/`admin_update_staff_user`. Plain additive tag (same shape as Payroll Officer/Store Manager) - widens [gmaConversations.js](docs/js/gmaConversations.js)'s init() gate to `isSuperUser || isConversationsStaff` and [nav.js](docs/js/nav.js)'s GMA Conversations link to the same pair; new checkbox in [user-setup.html](docs/user-setup.html)/[userSetup.js](docs/js/userSetup.js) to assign it per user. Note: GMA Conversations itself has no per-conversation "assign to user" feature yet (grepped for it - doesn't exist) - this only controls who can open the page at all.

- **Transfer Order Ship no longer blocks on "not enough stock" when Item Ledger writing for Transfer Orders is off**, per "can you do if the itemledger entries is false in the general setup for transfer order can you allow this to ship since we are not tie up on the item ledger entries" (surfaced trying to ship the remaining 5 of TR-GMA0000049's AQ-030 BlackSealant against the now-empty, post-wipe ledger). [transferOrders.js](docs/js/transferOrders.js)'s `shipTransferOrder` now checks `admin_get_item_ledger_transfer_posting_enabled` (the toggle from supabase_item_ledger_transfer_posting_toggle.sql) before running its pre-ship stock-shortage check - only enforced while that setting is on, since that's also when the server-side ledger trigger has any say over the shipment at all. Off, Ship goes straight through to claiming serials/updating the document, same as before the Item Ledger existed for transfers. No SQL change - reuses the existing toggle RPC.

- Added one-off [supabase_fix_transfer_tr_gma0000049_line2.sql](sql/supabase_fix_transfer_tr_gma0000049_line2.sql) - the first fix script silently skipped AQ-008 ClearSealant because its actual stored SKU has a stray space after the hyphen ("AQ-008- ClearSealant" vs. the other rows' "AQ-008-BlackSealant"), so the exact-match join found no row. This one matches on a space-stripped SKU comparison instead.

- Added one-off [supabase_fix_transfer_tr_gma0000049.sql](sql/supabase_fix_transfer_tr_gma0000049.sql) to manually restore Transfer Order TR-GMA0000049's real Qty Shipped/Received (one of the documents reverted to "To Ship" by the Item Ledger wipe's Step 5 - see the 2026-09-22 entry), per direct instruction: all lines fully received except AQ-030 BlackSealant, which has 5 of 10 still pending. Disables `TR_Transfer_Line_ItemLedger` first (same escape-hatch pattern as the wipe script) so the update doesn't try to post a shipment against the now-empty ledger's 0 balance at the From warehouse and get refused by the negative-stock check. Deliberately does NOT recreate matching Item Ledger entries for this real movement - flagged in the file's own header as a known gap, since the ledger has no record Amaya ever held this stock to begin with. Not run against the live DB from here - the user runs it.

## 2026-09-22

- **Added an on/off switch for Transfer Order Item Ledger posting, on General Setup**, per "can you post this routine for now? i mean add a setup under general setup to enable 'item ledger writing for transfer order'". New [supabase_item_ledger_transfer_posting_toggle.sql](sql/supabase_item_ledger_transfer_posting_toggle.sql) adds `ItemLedgerSetup."TransferPostingEnabled"` (default **true** - preserves today's already-live behaviour exactly as-is) and gates `_ile_transfer_line_post()` on it. New checkbox under a new "Item Ledger" section on [general-setup.html](docs/general-setup.html)/[generalSetup.js](docs/js/generalSetup.js) (`admin_get/set_item_ledger_transfer_posting_enabled`). When switched off: Ship/Receive still update the Transfer Order's own Qty Shipped/Received normally (documents still progress, no error) - the trigger just skips writing to the Item Ledger, which also means the "not enough stock at the From warehouse" check is skipped while off, since that check only runs as part of the ledger post. Gives a real UI switch for what previously required manually disabling the trigger in the SQL editor (exactly what the ledger test-data wipe script had to do for itself this session).

- **Fixed [supabase_item_ledger_wipe_test_data.sql](sql/supabase_item_ledger_wipe_test_data.sql) erroring out on a real run** ("ERROR: P0001: Variant ... does not belong to item 'SERVICES'"). Cause: Step 5's plain `UPDATE` of `Transfer_Line`'s Qty Shipped/Received wasn't a quiet reset - it fires `TR_Transfer_Line_ItemLedger` (supabase_item_ledger_hooks.sql), which POSTS a new ledger entry for the delta, i.e. the script was trying to write fresh entries into the ledger it had just wiped, and one hit a genuine bad variant/item link along the way. Step 5 now disables that trigger first, same escape-hatch pattern as the ledger wipe itself. Also corrected: `Items."QuantityInStock"` turns out to BE kept in step with the ledger (an INSERT trigger from supabase_item_ledger_sales.sql) - a `TRUNCATE` never fires that trigger, so it would have been left stale; new Step 7 recomputes it for every item via that file's own `_ile_sync_all_item_quantities()`.

- Added one-off [supabase_item_ledger_wipe_test_data.sql](sql/supabase_item_ledger_wipe_test_data.sql), per "if i want to clear the entries. should I only delete the item ledger entries for a fresh start?" -> "no i just want to wipe my testing data". A plain DELETE can't touch `ItemLedgerEntries` (it's append-only by trigger design), so this disables the guard triggers in one transaction, truncates the ledger + restarts its identity/transaction-number sequence, re-enables the triggers, then resets what would otherwise go stale: `PurchaseOrderLines.QtyReceived`, `Transfer_Line`'s Qty Shipped/Received (+ To Ship/To Receive), and clears the Physical Inventory Journal worksheet (its Qty. (Calculated) values are frozen ledger reads). Does not touch document headers' own Status column or already-posted/archived orders - flagged in the file's own header as a known gap if any test document's Status needs fixing by hand afterward. Not run against the live DB from here - the user runs it.

- **Fixed the automated "To Ship" Messenger reply sometimes quoting the wrong balance**, per "the balance automated response from Local pos to pancake is sometimes wrong" + follow-up "what im asking is the code from the local pos.. the problem is coming when they do status change on local pos". Two separate bugs, both in the Balance shown in that message:
  - **Formula bug (both portal and desktop)**: `Balance` was being read straight from Pancake's own `cod` field, which goes stale whenever a payment/order edit updates `money_to_collect`/`prepaid` without Pancake also recomputing `cod`. Now computed locally as `MoneyToCollect - AmountPaid` everywhere it's set: the 3 Supabase writers of `OnlineOrders."Balance"` (`admin_sync_online_orders_from_pancake`, `cron_sync_online_orders_from_pancake`, `admin_list_online_orders_live` in [supabase_pancake_manual_sync.sql](sql/supabase_pancake_manual_sync.sql), redeployed + backfilled via new [supabase_online_order_balance_local_calc.sql](sql/supabase_online_order_balance_local_calc.sql)) and the desktop's own order sync ([IntegrationEvents.cs](IntegrationEvents.cs)).
  - **Staleness bug (local POS specifically - this is what was actually being reported)**: the desktop app's `dbo.OnlineOrderHeader.Balance` (and the bound grid's copy of it) is only refreshed when the Online Orders form opens or staff click Sync - its auto-refresh timer is commented out, so there's no periodic catch-up. Marking an order To Ship never re-synced first, so the automated message could quote a balance that was stale by however long the form had been open, e.g. a payment collected mid-session. [OnlineOrdersForm.cs](OnlineOrdersForm.cs)'s `SendUpdateToCustomerForRowAsync` (used by the To Ship / Production Done flows) and the manual "Advise" button now call a new `IntegrationEvents.GetLiveOrderBalanceAsync` ([IntegrationEvents.cs](IntegrationEvents.cs)) that fetches the order straight from Pancake at message-build time and uses that live balance, falling back to the old cached grid/DB value only if that live call fails. **Needs [supabase_online_order_balance_local_calc.sql](sql/supabase_online_order_balance_local_calc.sql) run in Supabase, and a rebuild/redeploy of the desktop app.**

- **Made the Physical Inventory Journal's Search box behave more like BC's**, per "can you add search functionality on the physical journal.. same as the bc search functionality" (the title-bar search box already filtered the grid live; this adds the rest of BC's quick-filter behaviour around it). Added: a clear (x) button inside the box once you've typed something; a live "N of M" match count next to it; **Enter** selects and scrolls to the first matching line (like BC's quick filter jump-to-record); **Escape** clears the search. [physical-inventory-journal.html](docs/physical-inventory-journal.html)/[physicalInventoryJournal.js](docs/js/physicalInventoryJournal.js)/[bc-list.css](docs/css/bc-list.css) - client-side only, no database change.

- **Physical Inventory Journal: swapped the Category column for SKU, and added Print**, per "remove the category field. then can you show the SKU field. Also can you add a print functionality to print all the qty calculated". New [supabase_phys_journal_sku_column.sql](sql/supabase_phys_journal_sku_column.sql) drops `category_code`/`category_name` from `_ile_phys_journal_rows`/`admin_get_phys_journal_lines` and adds `sku` in their place, resolved the same fallback way category was (matched variant's SKU, then any other variant under the same item code, then the item's own SKU). Grid/FactBox/search/sort updated to match ([physical-inventory-journal.html](docs/physical-inventory-journal.html)/[physicalInventoryJournal.js](docs/js/physicalInventoryJournal.js)). New **Print** button on the action bar builds a print-only count sheet (Item No., Description, Variant, SKU, Location, Qty. (Calculated), plus a blank handwritten "Qty. (Phys. Inventory)" column and a Counted By/Date line) from whatever's currently loaded/filtered/sorted on screen, and opens the browser's print dialog - a plain browser print, not a PDF/report. New `#printArea`/`@media print` rules in [bc-list.css](docs/css/bc-list.css) hide the rest of the page and show only the sheet while printing.

- **Fixed the Physical Inventory Journal's new Category column coming back blank for most lines**, per screenshot + "can you ensure we flow the category base on the product/ variant category". Cause: Category gets stamped by the Pancake sync onto BOTH `Items` and `Variants` independently, and one can be populated while the other isn't (already true elsewhere - `staff_start_item_warehouse_stock_refresh` has to union both for the same reason) - the previous version only ever read `Items."CategoryCode"`, and only joined `Variants` at all for a line that has a formal VariantId, which is the minority of lines. New [supabase_phys_journal_category_from_variant_fallback.sql](sql/supabase_phys_journal_category_from_variant_fallback.sql) resolves it through a fallback chain instead: the line's own matched Variants row first, then any other Variants row under the same Item Code with a non-blank category (most recently synced), then `Items."CategoryCode"` - whichever level actually has it. Same `category_code`/`category_name` output fields, no JS/HTML change needed. If a line is still blank after this, that item genuinely has no category recorded anywhere in the portal (a real data gap to fix on Item Setup/re-sync, not a query issue).

- Narrowed the AI bot's ("Alice"/"Vic") wholesale scope further to exclude Sets/bundles, per "wholesale price applies only for individual tanks / stands and sumps, SET price cannot be apply as wholesale pricing its a different set package price". The WHOLESALE PRICING section of `buildSystemPrompt` ([chatbot-engine.ts](supabase/functions/_shared/chatbot-engine.ts)) now says explicitly: wholesale only ever applies to an *individual* Aquarium/Stand/Sump Filtration item, never a Set/bundle/package (own fixed all-in price, never wholesale-eligible even if it contains an aquarium/stand inside it) - she's told to quote a Set's own listed price and never its wholesale_price or count it toward the ₱5,000 minimum, even if one happens to be present on that catalog row. Prompt-only; no data/RPC change (`SET` is already its own Categories row, separate from `AQUARIUM`/`STAND`/`SUMP`). **Needs a redeploy of the chatbot edge functions.**

- **Added a Category column to the Physical Inventory Journal**, per "can you show the category field on the physical inventory journal". New `category_code`/`category_name` columns on `_ile_phys_journal_rows`/`admin_get_phys_journal_lines` (new [supabase_phys_journal_category_column.sql](sql/supabase_phys_journal_category_column.sql), from `Items."CategoryCode"` -> `Categories."Description"` - the same lookup Category Setup and the Calculate Inventory category filter already use); shown as a new sortable/searchable grid column and in the FactBox. Display-only - doesn't change Calculate Inventory's existing Category filter/dropdown or posting.

- **Added sortable columns to the Physical Inventory Journal grid**, per "can you allow to sort the physical inventory journal". Click any column header (Item No., Description, Variant, Location Code, Qty. (Calculated), Qty. (Phys. Inventory), Quantity) to sort by it, BC-style; click the same header again to reverse. Client-side only (sorts whatever's currently loaded/filtered by the search box), with a small ▲/▼ arrow on the active column. Blank "Qty. (Phys. Inventory)" always sorts to the bottom regardless of direction, so uncounted lines don't scatter to the top on a descending sort. [physical-inventory-journal.html](docs/physical-inventory-journal.html)/[physicalInventoryJournal.js](docs/js/physicalInventoryJournal.js)/[bc-list.css](docs/css/bc-list.css) - no database change.

- **Fixed "Calculate failed: canceling statement due to statement timeout" on the new "Qty. (Calculated) from Pancake (live)" option**, per screenshot showing the error on first real use. Cause: that version fetched every distinct Pancake product's stock sequentially inside ONE RPC call/statement, which blew through Supabase's API-role statement_timeout once more than a handful of products were involved (a function-level `SET statement_timeout` can't rescue this - the timer is already armed with the caller's role default before the function body runs). New [supabase_phys_journal_calculate_from_pancake_fix.sql](sql/supabase_phys_journal_calculate_from_pancake_fix.sql): `admin_calculate_phys_inventory` goes back to being purely ledger-based and fast (no HTTP inside it at all); the live Pancake read is now a separate step [physicalInventoryJournal.js](docs/js/physicalInventoryJournal.js) drives after Calculate finishes, one product per bounded RPC call (`admin_get_pancake_stock_for_product`, ~6s HTTP timeout - safely inside one small statement) with a "Fetching live Pancake stock... N/M products" progress message, applying results back via the fast, HTTP-free `admin_apply_phys_journal_calculated_qty`. `_ile_phys_journal_rows`/`admin_get_phys_journal_lines` now also return each line's Pancake product id/variation id so the client knows what to fetch. Same end result and same fallback-to-ledger behavior as before, just no longer able to time out the whole Calculate call. Run this file AFTER [supabase_phys_journal_calculate_from_pancake.sql](sql/supabase_phys_journal_calculate_from_pancake.sql) - it undoes that file's inline-fetch approach.

- **Added a "Qty. (Calculated) from Pancake (live)" option to Calculate Inventory** on the Physical Inventory Journal, per "can we add an option there to be able to pick the qty. calculated from the pancake first? this way we can get the actual inventory from pancake". New checkbox in the Calculate Inventory dialog ([physical-inventory-journal.html](docs/physical-inventory-journal.html)/[physicalInventoryJournal.js](docs/js/physicalInventoryJournal.js)); unchecked (default) is unchanged - Qty. (Calculated) comes from the portal's own item ledger. Checked, it instead reads each line's current stock straight from Pancake Cloud (`products/{id} -> variations -> variations_warehouses -> remain_quantity`, the same shape already used by [supabase_transfer_line_pancake_stock.sql](sql/supabase_transfer_line_pancake_stock.sql)), batched one HTTP call per distinct Pancake product rather than per line so a big run stays reasonably fast. Best-effort per item - anything Pancake can't answer for silently falls back to the portal's own ledger figure instead of failing the run. New [supabase_phys_journal_calculate_from_pancake.sql](sql/supabase_phys_journal_calculate_from_pancake.sql) adds the `p_from_pancake` parameter to `admin_calculate_phys_inventory`. Only changes where the reference number comes from - **Post still always diffs the typed count against the portal's own live balance at post time either way**, unaffected by this option.

- Added [supabase_repair_item_pancake_sku_collisions.sql](sql/supabase_repair_item_pancake_sku_collisions.sql), per "i think there are some more mismatch as per checking how can we correct it" (follow-up to the AS-014 investigation). Traced the mechanism further than the original fix: the old bug didn't just scramble Name/Price/Category, it also stamped the WRONG `ProductId` onto a row - and the FIXED sync matches ProductId before Code, so a plain re-sync can just as easily re-corrupt a poisoned row as heal it (both the real and the wrong product resolve to the same row in one pass, non-deterministic winner). The repair instead wipes every `Items."ProductId"` (Code, Name, Price, Category untouched - Code is never corrupted, it's always the lookup key) and runs one full sync, so every product resolves purely by its own exact Code - correctly healing every affected row in one pass, not just the ones already spotted. Includes a verification step re-running the SKU-collision check. Not run against the live DB from here - the user runs it.

- Added read-only [supabase_diagnose_item_code_mismatch.sql](sql/supabase_diagnose_item_code_mismatch.sql), per "IM CONFUSE why is the data on pancake and portal is now different" (Item Setup showed AS-014 as a sticker while Pancake showed AS-014 as an aquarium set-up). Checks in one run: whether the currently-live `admin_sync_items_from_pancake` is the old SKU-collision-prone version or the fix from [supabase_pancake_item_sku_match_fix.sql](sql/supabase_pancake_item_sku_match_fix.sql) (AS-014 was named there as an at-risk example), the portal's current row for the code, whether its SKU collides with another code's (the bug's exact shape), and what Pancake itself says live for that code and for the missing product's name via the existing `debug_explain_pancake_product_sync`/`debug_find_pancake_product` helpers. Diagnostic only - nothing writes.

- **Added an "Only categories flagged 'Include in Stock Sync'" filter to Calculate Inventory** on the Physical Inventory Journal, per "if the physical inventory journal > Calculate Inventory... if the category is yes then only that category will flow in". New checkbox in the Calculate Inventory dialog ([physical-inventory-journal.html](docs/physical-inventory-journal.html)/[physicalInventoryJournal.js](docs/js/physicalInventoryJournal.js)); unchecked (default) behaves exactly as before - every active item. Checked, it only pulls in items whose category has `Categories."IncludeInStockSync" = true` (the flag restored earlier today), combining with the existing single-Category dropdown and item-text filter rather than replacing them. New [supabase_phys_journal_calculate_stock_sync_filter.sql](sql/supabase_phys_journal_calculate_stock_sync_filter.sql) adds the `p_only_stock_sync_categories` parameter to `admin_calculate_phys_inventory`. Nothing else (Stock On Hand, the rest of the journal, sales posting) reads this flag.

- Scoped the AI bot's ("Alice"/"Vic") wholesale pricing offer to Aquarium/Stand/Sump Filtration only, per "when they offer wholesale minimum 5k this will cover only Aquarium / Stand and Sump filtrations only". Rewrote the WHOLESALE PRICING section of `buildSystemPrompt` ([chatbot-engine.ts](supabase/functions/_shared/chatbot-engine.ts)) with an explicit SCOPE rule: she now only asks retail-vs-wholesale when the customer is interested in an Aquarium/Stand/Sump Filtration catalog item, never for any other product line or for a custom aquarium/stand quote (`compute_aquarium_quote` has no catalog wholesale price at all). No data/RPC change - `public_search_items` already only returns a non-null `wholesale_price` for those three categories (`Categories."IsWholesaleApplicable"`, see `supabase_category_wholesale_flag.sql`); this just stops her from asking the question at all outside that scope instead of asking then quoting retail anyway. **Needs a redeploy of the chatbot edge functions.**

- **Added "Delete All Lines" to the Physical Inventory Journal**, per "in the physical inventory journal can I delete all the lines?" (a broad Calculate Inventory run had left 673 lines with no way to clear them besides one at a time). New button on the action bar clears every line in the current batch in one go, after confirming (and naming how many already have a count that would be lost) - it only clears the worksheet, never the ledger, since nothing here has posted yet. New [supabase_item_ledger_phys_journal_clear_batch.sql](sql/supabase_item_ledger_phys_journal_clear_batch.sql) (`admin_clear_phys_journal_batch`); [physical-inventory-journal.html](docs/physical-inventory-journal.html)/[physicalInventoryJournal.js](docs/js/physicalInventoryJournal.js) updated to match.

- **Restored the "Include in Stock Sync" checkbox on Category Setup**, per "i remember in the categories we have include on stock count right? can we revert that back with the data?" (clarified via question: UI only). New [supabase_category_restore_stock_sync_flag.sql](sql/supabase_category_restore_stock_sync_flag.sql) re-adds `include_in_stock_sync` to `admin_list_categories`/`admin_update_category_flags`; the column was never dropped when it was removed, so the old saved values reappear as-is. [category-setup.html](docs/category-setup.html)/[categorySetup.js](docs/js/categorySetup.js) show and save the checkbox again (colspan 6 → 7). It's reference-only - Stock On Hand and the Physical Inventory Journal are unchanged and still cover every item regardless of this flag.

- **Fixed both BC-style list pages leaving a slab of empty page below a too-short grid**, per "can we maximize all spaces.." on the new Physical Inventory Journal (visible with a large item count - the grid stopped at a guessed `calc(100vh - 360px)` that didn't match this page's shorter chrome, wasting most of the screen). [bc-list.css](docs/css/bc-list.css)'s `.bc-grid-wrap` max-height is now a pre-JS fallback only; a new `fitGridToViewport`/`fitJournalGrid`/`fitActiveGrid` helper in [physicalInventoryJournal.js](docs/js/physicalInventoryJournal.js) and [itemLedgerEntries.js](docs/js/itemLedgerEntries.js) measures the grid's actual position and sizes it to fill exactly to the bottom of the window, re-run on load, on window resize, and (ledger page) on every tab/filter-pane toggle. `.bc-body`'s `align-items` changed from `start` to `stretch` so the FactBox/filter pane reach the same bottom edge as the now taller grid. [item-ledger-entries.html](docs/item-ledger-entries.html) and [physical-inventory-journal.html](docs/physical-inventory-journal.html) gained ids (`ledgerGridWrap`/`balanceGridWrap`/`journalGridWrap`) on their grid-wrap divs for this to target. No RPC/behavior change.

- **Added a Physical Inventory Journal**, per "can you create me a physical inventory journal same as business central UI UX and behaviour? this way we can do physical count easily". New [supabase_item_ledger_phys_inventory_journal.sql](sql/supabase_item_ledger_phys_inventory_journal.sql) (table `PhysInventoryJournalLines` + RPCs) and a new page, [physical-inventory-journal.html](docs/physical-inventory-journal.html) / [physicalInventoryJournal.js](docs/js/physicalInventoryJournal.js), linked from Inventory (super users), reusing [bc-list.css](docs/css/bc-list.css). Unlike the existing CSV "Load or Count Stock" tool (a one-shot import), this is BC's on-screen worksheet: **Calculate Inventory** for a location (optionally by category/search) pulls in a line per item/variant with the ledger's current quantity as "Qty. (Calculated)" - a frozen reference that recalculating never overwrites once a line has been given a count; type what you counted straight into "Qty. (Phys. Inventory)" (Enter moves to the next line, like a BC datasheet); add a line by hand (**New Line**) or remove one (**Delete Line**); a FactBox shows the selected line's details plus live availability by location. **Post** posts one Positive/Negative Adjmt. per line with a real difference (document `PHYS-000123`) and clears every counted line from the worksheet - lines not yet counted stay for next time. Deliberate departure from literal BC: the posted quantity always diffs against the **live** ledger balance at the moment you click Post, not the frozen Qty. (Calculated) - so the result always lands exactly on what you counted even if stock moved in between. Work is grouped by a free-typed **Batch Name** (one per person/aisle/day), created implicitly the moment a line is added to it. No changes to the existing ledger, CSV stock-count tool, or sales posting. AI bot unaffected (staff-only page).

- Added a **Customer Message Timing** report, per "build reporting the timing of messages captured from our GMA conversations... track when customers are messaging us": new [message-timing.html](docs/message-timing.html) / [messageTimingDashboard.js](docs/js/messageTimingDashboard.js) (super users only, added to the Reports nav group) shows a day-of-week x hour-of-day heatmap of inbound customer messages on GMA Conversations' `ChatbotMessages`, plus busiest day/hour stat cards and a top-10 busiest-slots table, with an optional date filter. New [supabase_chatbot_message_timing_rpc.sql](sql/supabase_chatbot_message_timing_rpc.sql) (`admin_get_chatbot_message_timing`, mirrors `admin_get_order_confirmation_timing`'s day/hour bucketing in Asia/Manila time) - only counts `Role='user'` rows, not the bot's or staff's own replies. Limited to the last 60 days by the existing `ChatbotMessages` retention cron. Heatmap colors are a validated single-hue sequential ramp (dataviz skill) in new `.msg-heat-*` classes in [styles.css](docs/css/styles.css). Reporting-only; no bot behavior changed.

- Taught the AI bot ("Alice") to quote aquarium repairs, per direct follow-up to the Repair Calculator built earlier ([repair-calculator.html](docs/repair-calculator.html)). New `compute_repair_quote` tool in [chatbot-engine.ts](supabase/functions/_shared/chatbot-engine.ts) - a hand-ported copy of [repairCalculator.js](docs/js/repairCalculator.js)'s logic (must be kept in sync manually if that changes): glass panel replacement priced from the damaged panel's own area (worked out from the aquarium's overall L/W/H) at the live glass rate plus the configurable labor markup; resealing/leak repair priced by tank-size tier, both pulled live from `RepairPricingSetup`/`public_get_repair_pricing_setup`. Quote-only - proceeding routes to `escalate_to_staff` for drop-off/scheduling since there's no `create_order` path for repairs yet. Updated `buildSystemPrompt`, `log_capability_gap`'s description, and the escalation rules so repairs are no longer treated as an unhandled capability gap; other repair types (stand/electrical/filtration) still fall to `log_capability_gap`. Applies to every channel that shares this engine (Messenger, website widget, portal chat sandbox, Telegram) - **needs a redeploy of the chatbot edge functions**.

- Added read-only [supabase_diagnose_chatbot_followups.sql](sql/supabase_diagnose_chatbot_followups.sql) to troubleshoot the Messenger follow-up feature ("Vic" not sending abandoned-chat check-ins): checks current `ChatbotFollowUpSettings`, whether the `dispatch-chatbot-followups` cron job is alive/running, the `ChatbotFollowUps` queue (Pending/Sent/Skipped), and which conversations should currently be triggering a nudge. No behavior change - diagnostic only.

- **Item Ledger Entries page restyled to look and work like Business Central's list page**, per "make the ui/ux same as Business Central item ledger entries". New stylesheet [bc-list.css](docs/css/bc-list.css) (scoped to the page, light + dark) and a rewritten [item-ledger-entries.html](docs/item-ledger-entries.html) / [itemLedgerEntries.js](docs/js/itemLedgerEntries.js): title bar with Search and a filter-pane toggle; a flat action bar (New Adjustment, Load / Count Stock, Reverse Transaction, Refresh); the sales-posting notice as an amber/blue message bar; "Ledger Entries" and "Stock by Location" view tabs; a collapsible "Filter list by" pane (closed by default like BC, with the applied filters shown as removable chips); a compact grid with BC's column names (Posting Date, Entry Type, Document Type/No., Item No., Description, Variant, Location Code, Quantity, Correction, Entry No.), row selection (click or Up/Down) and a sticky total row; and a right-hand FactBox with the selected entry's details plus "Item Availability by Location". Item No. is a drill-down to that item's ledger history. Adjustment, stock-count upload and reversal are now dialogs (reversal asks for its reason in a dialog instead of a browser prompt). Reverse is enabled only for a selected entry that isn't already reversed / a correction. No database or RPC changes - same functions, same data. AI bot unaffected.

## 2026-09-21

- **Dropped the "Include in Stock Sync" category setup and made the ledger track every item**, per "no need to rely on the setup 'Include in stock sync' under categories ... the portal to be the source of truth ... everything to be monitored as of the moment". (1) New [supabase_category_drop_stock_sync_flag.sql](sql/supabase_category_drop_stock_sync_flag.sql): `admin_list_categories`/`admin_update_category_flags` lose the flag; Category Setup ([category-setup.html](docs/category-setup.html), [categorySetup.js](docs/js/categorySetup.js)) no longer shows it (run the SQL **before** deploying the JS); the Pancake refresh RPC now covers every product (only used by the optional seed). The column stays in the table, unused. (2) Stock On Hand now lists **every active item** - one with nothing posted shows 0 - with a "Hide items with no stock" checkbox; its Category filter lists all categories ([supabase_item_ledger_hooks.sql](sql/supabase_item_ledger_hooks.sql), [stockOnHand.js](docs/js/stockOnHand.js)); Create Purchase Order now requires a Warehouse filter when a listed item has no warehouse. (3) **Starting stock no longer needs Pancake**: new [supabase_item_ledger_stock_count.sql](sql/supabase_item_ledger_stock_count.sql) + a "Load or Count Stock (CSV)" section on the Item Ledger Entries page - upload Item / Variant / Warehouse / Quantity (item by code or SKU, warehouse by name or ID), preview, post. It sets each stock key to the counted quantity by posting only the *difference*, so it works as opening balances, as a later stock take, and re-uploading the same file changes nothing; all-or-nothing, errors block posting. (4) Sales posting is now switched on by a "Start Posting Sales From Now" button on that page (`admin_start_item_ledger_sales_posting`, once only, refused on an empty ledger; also recomputes `Items."QuantityInStock"`) instead of by the seed. The Pancake-snapshot seed ([supabase_item_ledger_seed_opening_balances.sql](sql/supabase_item_ledger_seed_opening_balances.sql)) is kept only as an optional shortcut. AI bot code unchanged.

- Taught the AI bot ("Alice") to ask retail vs. wholesale and enforce a ₱5,000 per-transaction wholesale minimum, per "ask the customer if they want wholesale or retail pricing... wholesale price minimum is 5k per transaction": rewrote the WHOLESALE PRICING section of `buildSystemPrompt` ([chatbot-engine.ts](supabase/functions/_shared/chatbot-engine.ts)) - it asks once when a customer shows buying interest (replacing the old "never volunteer wholesale" rule), quotes `wholesale_price` only when wholesale-priced items total ₱5,000+, and offers retail or adding items when short. Because `create_order` always records the retail price, wholesale orders are handed to staff via `escalate_to_staff` instead of being placed by the bot. The ₱5,000 minimum is prompt-only (not enforced in code). Needs a redeploy of the chatbot edge functions.

- Taught the AI bot ("Alice") the hang-on-back (HOB) filter rule, per "check if the filtration is hang on back the aquarium supposed to be rimless": in `buildSystemPrompt` ([chatbot-engine.ts](supabase/functions/_shared/chatbot-engine.ts)) it now asks what filtration the customer plans to use when advising tank size / quoting a custom aquarium, and if it's HOB it treats the tank as rimless (`rimless=true`) and explains the rimless glass-thickness rule; the `compute_aquarium_quote` `rimless` param description says the same. No pricing math changed (the rimless safety rule was already in `calculateCustomAquarium` and the web calculator). **Needs a redeploy of the chatbot edge functions** (`chatbot-web-reply` and the Messenger one) to take effect.

- **Sales now take stock out of the Item Ledger** (chosen: "On Confirmed"), completing the move off Pancake. New [supabase_item_ledger_sales.sql](sql/supabase_item_ledger_sales.sql) (run after the two ledger files): a per-order *reconcile* (`_ile_reconcile_online_order`) brings the ledger in line with what the order currently says - lines confirmed/changed/cancelled post the difference as `Sale` entries from `OnlineOrders."LocationID"`, per variant; running twice posts nothing. Runs every minute via pg_cron (`cron_post_online_order_sales`) over orders changed since last reconciled, plus `admin_post_online_order_sales_now`. Counts any stored status except cancel/remove/return/refund; walk-ins included. **Off until the opening-balance seed sets `ItemLedgerSetup."SalesPostingStartUtc"`** - only orders confirmed after that post, so pre-cutover sales (already in the Pancake snapshot) aren't deducted twice. Also: a trigger keeps `Items."QuantityInStock"` = the item's ledger total, so Order Now's "In stock", the AI bot's stock answers and Item Setup follow the ledger with no reader changes (the seed recomputes it for every item once). Sales Order entries can't be reversed by hand (they'd be re-posted) - cancel/edit the order instead. [supabase_item_ledger_seed_opening_balances.sql](sql/supabase_item_ledger_seed_opening_balances.sql) now also switches sales posting on; new read-only [supabase_diagnose_item_ledger_sales.sql](sql/supabase_diagnose_item_ledger_sales.sql). Known limits: a line *removed* from an order in Pancake stays in `OnlineOrderLines` (the sync never deletes) and keeps counting; lines that don't match a stocked item (custom aquariums, services) are ignored; if the desktop POS still pushes `QuantityInStock` to Supabase it would overwrite the ledger-derived figure. AI bot code unchanged - it reads `QuantityInStock`, which now follows the ledger.

- **Removed Pancake from inventory - the portal's Item Ledger is now the source of truth**, per "my goal here is to remove the hook of relying the inventory in the pancake system. we are building our own inventory system through our portal". **Supersedes the "Wired the stock-moving workflows" entry below**, which had kept every Pancake push/gate and only added the ledger beside them; [supabase_item_ledger_hooks.sql](sql/supabase_item_ledger_hooks.sql) is rewritten accordingly (safe to run whether or not the earlier version was). (1) `staff_receive_purchase_order_lines` no longer calls Pancake: it bumps `QtyReceived` and posts a `Purchase` ledger entry in one transaction, against the line's own variant - so the Pancake Sync panel, Check Stock, "Failed - verify in Pancake before retrying" warning and `staff_confirm_purchase_order_receipt_without_sync` (dropped) are gone from `docs/purchase-orders.html`/`purchaseOrders.js`. (2) Transfer Order Ship/Receive no longer sync/complete in Pancake (Pancake Sync panel + `friendlyPancakeErrorMessage` removed from `transfer-orders.html`/`transferOrders.js`): the `Transfer_Line` trigger posts the ledger entries, Ship is **refused if the From warehouse doesn't have the stock** (checked in the UI before serials are claimed, and again by the trigger), and "Available (Pancake)" is now "Available" from the ledger via new `staff_get_transfer_line_stock`. (3) Stock On Hand now sums the ledger (`staff_list_item_warehouse_stock`/`staff_list_stock_sync_categories` redefined, same signatures) - "Refresh from Pancake" removed. (4) [supabase_item_ledger_entries.sql](sql/supabase_item_ledger_entries.sql): `_ile_post` now normalises the stock key (variant only counts when the item has 2+ variants resolving to one Items row; otherwise the item code alone is the shelf), can refuse negative stock (transfers, negative adjustments) with a per-key advisory lock, and reversing a Purchase Receipt hands the quantity back to the PO line (Transfer entries can't be reversed - correct with an adjustment). (5) New one-time [supabase_item_ledger_seed_opening_balances.sql](sql/supabase_item_ledger_seed_opening_balances.sql) loads opening balances from the last Pancake stock snapshot (preview -> seed -> skipped report; refuses to run twice; cutover order in its header). The posting-failure queue from the earlier version is removed (nothing external to fail now). (Sales and `Items."QuantityInStock"` were done in the entry above.)

- Wired the stock-moving workflows into the Item Ledger, per "i want to involve every feature inside the item ledger entry ... every item that is moving same as business central. Also i want per variant be covered". New [supabase_item_ledger_hooks.sql](sql/supabase_item_ledger_hooks.sql) (run after [supabase_item_ledger_entries.sql](sql/supabase_item_ledger_entries.sql), **before** deploying the JS): (1) `staff_receive_purchase_order_lines` and `staff_confirm_purchase_order_receipt_without_sync` now post a `Purchase` entry for every quantity they add to `QtyReceived`, at the line's warehouse and against the line's own variant - the receive function is rebuilt from the latest repo version and **restores the per-variant Pancake lookup** that `supabase_purchase_order_pancake_stock_verification*.sql` had dropped (if those ran after `..._receive_variant_stock.sql`, receiving was stocking Pancake against the item's representative variation); (2) new trigger on `Transfer_Line` posts `Transfer` entries - Ship = minus at the From warehouse, Receive = plus at the To warehouse (BC-style, in transit between) - off the "Qty Shipped"/"Qty Received" deltas, only for portal-shipped lines; new `Transfer_Line."Last Actor"` column written by `docs/js/transferOrders.js` for attribution; (3) automatic postings go through `_ile_post_safe`, which on any error queues the movement in `ItemLedgerPostingFailures` instead of failing the receipt/transfer (both only commit after Pancake already accepted the movement) - shown as a banner with Dismiss on the ledger page. Also: ledger + balances search by variant name/SKU, new "Combine variants" toggle on Stock Balance, `?search=` deep link on the page, posting dates use the Manila day (`_ile_today`). **Sales are not wired yet** - waiting on which order status should take stock out. AI bot ("Alice") unaffected.

- Fixed the wholesale pull failing on its first live try with `401 permission denied for table Items` - the POS uses the anon/publishable Supabase key and anon has no SELECT on `public."Items"` (only the order/serial/transfer/expense tables were opened to anon). New [supabase_pos_item_wholesale_price_pull.sql](sql/supabase_pos_item_wholesale_price_pull.sql) adds `pos_list_item_wholesale_prices(p_limit, p_offset)` (SECURITY DEFINER, granted to anon): only `Code` + `WholesalePrice`, only for items that have one - not a table-wide grant, since that key is committed in this repo and would expose Cost etc. `SyncItemWholesalePricesFromSupabaseAsync` now POSTs to that RPC and **no longer blanks a local price when the portal has none** (portal blank = "not entered yet", so overwriting would wipe every wholesale price staff keep locally - the portal has probably never been seeded). Trade-off: clearing a price in the portal doesn't clear it on the POS. **Correction to the entry below:** the same 401 means the POS's own `SyncItemsToSupabaseAsync` (its existence check is a GET on the same table) almost certainly fails at its first row - swallowed by the timer's try/catch - so the POS has probably NOT been pushing Items/Price/Cost/Wholesale to Supabase, and portal Items come from the Pancake cron sync only. That makes the "POS push reverts portal edits" reasoning below likely overstated; removing WholesalePrice from that push is harmless either way. Not verified against live data.

- Made the Web Portal the owner of item **Wholesale Price** and had the desktop POS pull it, per "it should be local POS pull from Supabase" - `OnlinefunctionsEvents.cs`: new `SyncItemWholesalePricesFromSupabaseAsync` (Supabase -> `dbo.Items.WholesalePrice`, paged, update-only, changed rows only) and `WholesalePrice` removed from the Items push (`OptionalItemColumns`). `MainForm.cs`: called from `MasterDataSyncTimer_Tick` and from the existing **Sync Items** button (`RunManualProductSyncAsync`, header bar + Items grid), which now also reports "Wholesale prices updated: N". New optional [supabase_item_wholesale_price_zero_to_null.sql](sql/supabase_item_wholesale_price_zero_to_null.sql) (preview first) turns any `0` wholesale prices in the portal into blank. POS project compiles; not yet run against live data. Local POS edits to wholesale (item form, wholesale Excel import) are overwritten by the next pull when the portal has a value for that item. AI bot ("Alice") code unchanged - it reads `Items."WholesalePrice"` from Supabase.

- Fixed the first live try of the price push failing with Pancake HTTP 422 `[retail_price]: is invalid; [price_at_counter]: is invalid` - the RPC sent 2-decimal numbers (`150.00`); [supabase_item_price_pancake_push.sql](sql/supabase_item_price_pancake_push.sql) now rounds to a whole number and sends a JSON integer (matches the `round(...)::int` used for Pancake order-line prices elsewhere), and the rejection message now includes the request body sent. Fractional prices are rounded on save and the Item Card says so. The integer-vs-decimal cause is inferred from the error, not confirmed by Pancake's docs.

- Made prices editable from Item Setup and pushed them to Pancake **per variant**, per "Portal > Pancake then Pancake will update the local POS" / "it should be per variant since our system supports variants per item" - flow is Portal -> Pancake (`PUT /products/{id}` with one variation's `retail_price`/`price_at_counter`; other variations untouched) -> local POS (already pulls Pancake's `retail_price` every 5 min via `SyncProductVariationsAsync`, so **no POS change**) -> Supabase. New [supabase_item_price_pancake_push.sql](sql/supabase_item_price_pancake_push.sql): shared internal `_pancake_push_variation_price`, `admin_set_variant_price` (one variation; also updates `Items.Price` when that variant is the item's representative one, matching what the sync writes) and `admin_set_item_price` (items with 0/1 variants; refused for several). Pancake first, portal (`Items`/`Variants`.`Price`) only if Pancake accepts, so an error changes nothing. `docs/item-setup.html`, `docs/js/itemSetup.js`, `docs/css/styles.css`: Item Card > Prices has a Price input + Save (locked, with a pointer, when the item has several variants); Item Card > Variants shows a Price input + Save on each row when there are 2+ variants. New read-only [supabase_diagnose_item_retail_price_override.sql](sql/supabase_diagnose_item_retail_price_override.sql): Order Now/the bot/order price validation quote `coalesce("RetailPrice","Price")`, so an item with a stale `RetailPrice` would ignore the new Price - the save warns when that's the case. **Pancake endpoint taken from a third-party mirror of Pancake's API docs, not yet tried against the live shop - test on one item first.** AI bot ("Alice") code unchanged: it reads `Items`/`Variants` prices from the DB, so it follows the new price (subject to the `RetailPrice` caveat above).

- Widened the Description column on the New Purchase Order lines grid, per "can you make the description wider on the PO?" - Description 12% -> 22% (Item 16% -> 14%, Variant 11% -> 10%) and the grid's minimum width 940px -> 1080px so the other columns don't get squeezed (sideways scroll below that). `docs/purchase-orders.html`, `docs/css/styles.css`. Front-end layout only - AI bot unaffected.

- Added Item Ledger Entries (Business Central-style stock ledger), per "i want to be able to track all inventory movements" / "implement item ledger entry in this portal first" - portal-owned, no Pancake dependency. New [supabase_item_ledger_entries.sql](sql/supabase_item_ledger_entries.sql): append-only `ItemLedgerEntries` (one signed Quantity per row, BC entry types, `TransactionNo` grouping, update/delete/truncate blocked by trigger), internal `_ile_post` posting engine (not callable from the portal), `admin_post_item_adjustments` (manual +/- adjustments, also how opening balances get loaded), `admin_reverse_item_ledger_transaction` (corrections post an opposite entry, never an edit), and `admin_list_item_ledger_entries` / `admin_get_item_ledger_balances` (ledger view + on-hand computed from it). New page `docs/item-ledger-entries.html` + `docs/js/itemLedgerEntries.js`, linked under Inventory for super users. **Foundation only** - PO receiving, Transfer Order ship/receive and sales are not yet wired to write ledger entries, so stock moved by those isn't in the ledger yet. AI bot ("Alice") isn't affected - no customer-facing change.

## 2026-09-20

- Stopped a payroll run from double-including an employee already covered by another run for an overlapping period, per "if the employee already create a payroll run can you not include them on the next payroll run with the same coverage" - previously nothing stopped two runs both reading the same timesheet days and double-paying Base Pay/Overtime for them. New [supabase_payroll_run_skip_already_covered.sql](sql/supabase_payroll_run_skip_already_covered.sql) adds one more `not exists` to `admin_create_payroll_run`'s employee-seeding insert (same silent-skip precedent as the existing "skip employees with no timesheet entries in this period" check) - checked by date overlap against any other run's `PeriodStart`/`PeriodEnd` regardless of Draft/Finalized status; deleting that other run frees the employee up again automatically since `PayrollRunLines` cascades away with it. If every employee in the pay cycle is already covered, run creation now fails with a clearer message covering both reasons (no timesheet entries, or already covered).

- Added a "Projected funding needed" panel to the Timesheets page, per "show projected cash needed and digital needed to pay" so the officer knows how much Cash/Digital to prepare before actually creating the run - shown two places: an always-visible box on the main page itself (mirrors what "Create Payroll Run from This Week" defaults to - Weekly cycle, the currently-shown week - refreshes on week navigation/Save All, per "i cannot see it on the timesheets" after the first pass only put it inside the New Payroll Run modal), plus the modal's own version reflecting whatever Pay Cycle/period is actually picked there (needed for SemiMonthly, which isn't a plain week). New [supabase_payroll_projected_run_requirement.sql](sql/supabase_payroll_projected_run_requirement.sql) adds `admin_get_projected_payroll_run_requirement`, which runs the real `admin_create_payroll_run` calculation inside a savepoint and always rolls it back (rather than hand-porting its Hourly/Semi-Monthly/Weekly/overtime/Cash-Advance math a second time, which risks drifting out of sync the same way the AI bot's separate aquarium calculator copy did) - so the projection is guaranteed to match what a real run would compute. Wired into `docs/js/payrollTimesheets.js`/`docs/payroll-timesheets.html`.

## 2026-09-19

- Pivoted the group-chat-with-Alice feature to live entirely inside the portal, per "i want alice and the GC to live only in the portal" (no external Telegram app). Discovered the portal already has a full Messenger-style DM widget (`docs/js/chat.js`, schema in `supabase_portal_chat_tables.sql`) with `IsGroup` already reserved for a "planned follow-up" group feature - built that follow-up now instead of a bespoke new page:
  - New [supabase_portal_chat_groups_and_alice.sql](sql/supabase_portal_chat_groups_and_alice.sql): adds `create_group_conversation()` (multi-member group creation) and a synthetic `StaffUsers` row for `'alice'` (`IsActive=true`, unguessable random password hash - shows up as a normal contact in the chat directory but can never actually log in), plus `is_alice_conversation_member()`.
  - New `supabase/functions/portal-chat-alice-reply/index.ts`: generates Alice's replies inside portal chat, reusing the same shared engine as Messenger/website Alice in `simulate: true` mode (same safety net as the AI Bot Sandbox - no real orders/escalations/CRM saves from here). DM her directly and she always replies; add her to a group and she only replies when a message contains "@Alice" - re-verified server-side, not just trusted from the client.
  - `docs/js/chat.js`: added a "New Group" tab (multi-select staff + group name) alongside the existing 1:1 "New Message" flow, sender-name labels on group message bubbles, and the client-side trigger that calls the new function and broadcasts Alice's reply live to everyone in the conversation. New `.chat-new-tabs`/`.chat-msg-sender` styles in `docs/css/styles.css`.
  - The earlier Telegram-based approach (`supabase_telegram_alice_messages.sql`, `supabase/functions/telegram-alice-webhook`) stays deployed but is now dormant/unused (its webhook was never registered with Telegram) - left in place rather than deleted in case it's wanted later; say the word if you'd rather it be removed.
- Added @mention autocomplete to the portal chat thread input, per "when I put @ there are no users to be tagged" - typing "@" now opens a dropdown of the OPEN conversation's other members (filtered as you keep typing, navigable with arrow keys/Enter/Tab, Escape to dismiss) instead of requiring staff to already know someone's exact username, including Alice's. `docs/js/chat.js` (candidate list populated from the conversation's real membership each time a thread is opened) + new `.chat-mention-dropdown`/`.chat-mention-item` styles in `docs/css/styles.css`.
- Made URLs inside portal chat messages clickable, per "can you make the link clickable always" - Alice's quote replies often end with a drawing/preview link (`compute_aquarium_quote`'s URL) that was previously just inert text. New `chatLinkify()` in `docs/js/chat.js` (escapes the message first, then wraps bare URLs in a real `<a target="_blank">`, trimming trailing sentence punctuation off the link) + link-color styles in `docs/css/styles.css` for both message bubble colors.
- Restricted 1:1 DMs with Alice to super users only, per "staff can only tag alice on a GC, dont allow the non super user staff to talk to alice directly" - any staff can still @mention her inside a group they create, just not DM her directly. Enforced two layers: `docs/js/chat.js` hides her from the "Message" tab's directory for non-super users (still shown under "Group"), and `supabase/functions/portal-chat-alice-reply` refuses to generate a reply for a non-group conversation unless `is_admin_authorized` passes - the real enforcement point, since `get_or_create_dm_conversation` (supabase_portal_chat_tables.sql) never re-verifies identity at all.
- Scaled Resealing/Leak Repair pricing by tank size instead of one flat fee for any size, per "can you calculate the reseal - what is the best figure?" -> confirmed to scale by size, with no real cost data to calculate from, so this is a defensible-but-generic starting suggestion (~1.5x per tier, anchored on the existing P500 default as the smallest tier) meant to be sanity-checked against real material/labor costs, not a verified calculation: up to 20 gal (P500, unchanged default) / 21-50 gal (P800) / 51-100 gal (P1,200) / 101-150 gal (P1,800) / 151+ "monster" tanks (P2,500 starting point, recommend on-site verification). New [supabase_repair_resealing_tiers.sql](sql/supabase_repair_resealing_tiers.sql) extends `RepairPricingSetup` with the four new tier columns; all five are now editable on the Pricing Setup page. `docs/js/repairCalculator.js` picks the tier from the aquarium dimensions already entered for Panel Replacement (estimated gallons = L x W x H / 231) - resealing now requires dimensions to price, same as Panel Replacement.
- Repair Calculator: removed the "Glass rate" line from the Panel Replacement summary per "dont show the glass rate" - just lists each checked panel's price and the total now. Moved the aquarium Length/Width/Height fields to a shared spot above both tabs (used by Panel Replacement's math either way) and replaced Resealing/Leak Repair's free-text "Tank Size (gallons)" field with those same dimension inputs, per "let the user fill in the dimension of the tank to reseal" - the flat fee itself is unchanged (size doesn't affect resealing price), but the summary/spec text now shows the tank's actual dimensions and estimated gallons instead of a manually-typed size. `docs/repair-calculator.html`/`docs/js/repairCalculator.js`.
- Built a real `compute_sticker_quote` tool - Alice previously had NO tool at all for sticker/accessory/background pricing (Plain Sticker, Tiles Sticker, Acrylic, Allum TopCover, Rubber Matting, Glass, Marine Plywood, Laminated Plywood), so she was reciting stale/wrong static text instead (caught quoting a "Paint style - P50/sq ft" that doesn't exist anywhere in the real pricing table). New tool ports `calculateStandaloneSticker` from `docs/WebAquariumCalculator/custom-aquarium-calculator.js` into `chatbot-engine.ts` (same hand-ported-copy pattern as the aquarium quote, live `StickerPricingSetup`/`GlassPricingSetup` rates, verified against the real calculator's own output for Plain/Tiles/Rubber Matting/Glass-repair cases - all matched exactly) plus a missing `ceilNearest10` helper the port needed. Updated the GROUNDING RULES to call out accessory/sticker quotes explicitly, same as aquarium/delivery. Redeployed all five functions bundling the shared engine. **Still worth checking:** the old static sticker pricing text may still be sitting in AI Bot Setup's Additional Notes/Custom Directions fields - worth reviewing/removing that now that a live tool exists, so she's not tempted to recite the old numbers instead of calling it.
- Allowed Alice to share an item's wholesale price, per direct request ("allow sharing wholesale price only") - a deliberate, narrow carve-out from the previous blanket "never mention wholesale pricing" rule, with two guardrails confirmed before building: (1) only for wholesale-eligible categories (Aquarium/Stand/Sump, the existing Category Setup flag), enforced at the DATA layer in the new [supabase_search_items_wholesale_price.sql](sql/supabase_search_items_wholesale_price.sql) (adds `wholesale_price` to `public_search_items`, always null for a non-eligible category regardless of what's in Items.WholesalePrice), and (2) only mentioned when the customer explicitly asks about wholesale/bulk pricing, never volunteered - enforced via a new "WHOLESALE PRICING" system prompt section (`chatbot-engine.ts`). Cost stays completely excluded/banned either way, unchanged. Redeployed all five functions bundling the shared engine.
- Fixed a real bug in `buildGlassPriceLookup` (shared by Order Now, the staff Aquarium Calculator, Stickers, and the AI bot's quote tool): it read field names (`units`/`pricePerSqFt`/`uom`) that don't exist on what `public_get_glass_pricing()` actually returns (`thickness`/`price_per_sqft`, no `uom` column), so it always silently fell back to hardcoded defaults - live edits from the portal's Glass Pricing Setup page never actually applied anywhere. Fixed in both `docs/WebAquariumCalculator/custom-aquarium-calculator.js` and the AI bot's hand-ported copy in `supabase/functions/_shared/chatbot-engine.ts` (previously a deliberate faithful copy of the same bug, now fixed on both sides so they stay consistent AND honor live pricing). Redeployed all five functions bundling the shared engine.
- Reworked the Repair Calculator's Panel Replacement mode from one panel-location dropdown + single width/height into a checkbox per side (Bottom/Front/Back/Left/Right) with its own width/height inputs, per "let the user input the dimensions, then add a checkbox... so we can choose multiple panels" - each checked panel is priced separately (its own area x the shared glass rate + markup) and summed into one total, since a real repair job can involve more than one damaged panel at once. `docs/repair-calculator.html`/`docs/js/repairCalculator.js`.
- Simplified that further per "just let the user input the actual aquarium dimension then we can work from that dimension" - replaced the five separate per-panel width/height inputs with THREE shared fields (aquarium Length/Width/Height, entered once), and each checked panel's own area is now derived automatically using the exact same convention `getGlassAreaSqFt` uses for a full build (Bottom = Length x Width, Front/Back = Length x Height, Left/Right = Width x Height) - verified the derived panel areas sum to the identical total `getGlassAreaSqFt` would compute for the same tank.
- Added a new **Repair Calculator** (`docs/repair-calculator.html`/`docs/js/repairCalculator.js`), per "can we implement repair feature?" - scoped as a pricing calculator only (no job tracking), like the existing Custom Stand/Aquarium Calculator pages. Two repair types: Panel Replacement (damaged panel's area x the live Glass Pricing Setup rate + a configurable labor markup %) and Resealing/Leak Repair (a configurable flat fee). New [supabase_repair_pricing_setup.sql](sql/supabase_repair_pricing_setup.sql) (`RepairPricingSetup` table + public/admin RPCs) and a new "Repair Pricing Setup" section on `docs/pricing-setup.html`/`docs/js/pricingSetup.js` to edit both settings. Added to the Calculators nav group and the Store Manager page allowlist (`docs/js/nav.js`/`docs/js/auth.js`). **Alice does not know about repairs yet** - this was built as the system-side feature first per direct instruction ("build the feature first... so Alice can adopt after building"); teaching her about it (a new `compute_repair_quote` tool, system prompt updates) is a separate follow-up.
- Gave Alice an honest way to say "I don't know that yet" instead of implying she already handled something she didn't (seen when staff asked about aquarium REPAIR service pricing, which she has no knowledge of, and she replied as if it had already been sent to staff for a price). New `log_capability_gap` tool (`supabase/functions/_shared/chatbot-engine.ts`) - distinct from `escalate_to_staff` (for things she CAN handle but need a human to finish) - for requests she genuinely has no knowledge/pricing/tool for at all. New system prompt section ("WHEN YOU DON'T ACTUALLY KNOW / CAN'T HELP") tells her to say so plainly (Taglish example included) and log it, never claim it was forwarded when nothing was. New [supabase_bot_capability_gaps.sql](sql/supabase_bot_capability_gaps.sql) (`BotCapabilityGaps` table + admin list/status RPCs, seeded with this exact repair-pricing incident) and a new "Capability Gaps / Enhancement Requests" review table on `docs/ai-bot-setup.html`/`docs/js/aiBotSetup.js` so staff can actually see and triage the backlog instead of it being invisible. Redeployed all five functions that bundle the shared engine (`facebook-messenger-webhook`, `chatbot-sandbox-reply`, `chatbot-web-reply`, `telegram-alice-webhook`, `portal-chat-alice-reply`).

- Added a Terms & Conditions / Cancellation & Refund Policy page (`docs/terms-and-conditions.html`), linked from the homepage and Privacy Policy footers, including the DTI business registration statement (RSPETSTOP PET STORE, DTI Cert. No. 6230461) — for Facebook Business Verification.
- Added "How We Quote Delivery" and "Safety Rules We Auto-Check" sections to the Order Now page's Help modal (`docs/order-now.html`).
- Added a standalone Help page (`docs/help.html`, same style as Terms & Conditions) covering how to order, how we quote a custom build/delivery fee, and safety rules — linked from the homepage and other legal-page footers, so it's reachable outside the Order Now wizard too.
- Added a "Choosing a Lalamove Vehicle for Your Order" guide with the customer's requested disclaimer, to both `docs/help.html` and the in-app Help modal in `docs/order-now.html`.
- Reworked that guide into a "Complete Setup Of / Mode of Transport" table (50 gallons -> MPV, 75 gallons -> L300, per user's example) in both places.
- Expanded the table with a full row list from 10 to 200 gallons, leaving Mode of Transport blank ("—") except two known rows, pending the user filling in the rest.
- Replaced the per-gallon rows with the user's final "Advisable Mode of Transportation" ranges: 10-25g Sedan, 30-100g L300, 120-200g Lalamove "Closed Van" or in-house delivery — in both `docs/help.html` and the Order Now Help modal.
- Split the 30-100g row into 30-50g MPV and 50-100g L300, per user correction.
- Added a second "Aquarium Only" table (betta tanks/20g Motorcycle, 25-50g Sedan, 60-75g MPV, 90-120g L300, monster tanks -> in-house delivery) alongside the existing "Complete Setup" table, in both `docs/help.html` and the Order Now Help modal.
- Improved the transport-guide tables' UX: bordered rounded card wrapper, zebra-striped rows, and a colored pill badge for the vehicle value, instead of plain unbordered rows. Added `.transport-table*`/`.transport-badge` classes to `docs/css/styles.css` (light/dark POS theme) and matching `.legal-table*`/`.legal-table-badge` styles inline in `docs/help.html` (dark landing theme).
- Added a public website AI chat widget ("Alice"): a new anonymous-callable Edge Function `supabase/functions/chatbot-web-reply/index.ts` reuses the exact same bot engine (`supabase/functions/_shared/chatbot-engine.ts`) and the real `ChatbotConversations`/`ChatbotMessages` tables as the Facebook bot, keyed by a browser-generated `web:<uuid>` id instead of a Facebook PSID - so website conversations show up in the existing GMA Conversations staff inbox automatically. Real actions (escalate to staff via Telegram, save_customer_info, create_order/Pancake push) all run for real, not simulated. Added a self-contained front-end widget `docs/js/chatWidget.js` (floating bubble + panel, no login required) and wired it into the five public pages: `docs/index.html`, `docs/order-now.html`, `docs/help.html`, `docs/terms-and-conditions.html`, `docs/privacy-policy.html`. Deployed and smoke-tested.
- Known v1 limitations: `send_item_image` silently no-ops for web visitors (no Facebook Send API to push a photo through - the bot just describes the item in text instead); `schedule_follow_up`'s later delivery attempt via Facebook will fail silently for web visitors, though the follow-up text still lands in their stored history for their next visit; no CAPTCHA/stronger abuse protection beyond the existing 15-messages/5-minutes rate limit and a UUID format check.
- Added "Hole for Aquarium" (qty, flat ₱150/each, configurable) and "Divider" (qty, priced as the aquarium's own glass rate for a Width x Height panel + 20%) to the Order Now custom Aquarium builder. New `sql/supabase_aquarium_extra_pricing.sql` (AquariumExtraPricingSetup table + public/admin RPCs), new "Aquarium Extras Pricing" section on `docs/pricing-setup.html`/`docs/js/pricingSetup.js`, and pricing logic + two new qty inputs in `docs/WebAquariumCalculator/custom-aquarium-calculator.js`/`docs/order-now.html`/`docs/js/orderNow.js`. Verified with manual calculations (holes, divider, and configurable hole price override all correct).
- Added a standing CLAUDE.md instruction ("Keep the AI bot in sync") to update the bot's knowledge/tools whenever a customer-facing feature/price/policy changes, and immediately applied it: ported the same Hole/Divider pricing into the AI bot's `compute_aquarium_quote` tool (`supabase/functions/_shared/chatbot-engine.ts` has its own separate TS copy of the aquarium calculator, not a shared import - both now stay in sync manually). Added `hole_count`/`divider_count` to the tool's schema and to the system prompt's quoting instructions. Redeployed `facebook-messenger-webhook`, `chatbot-sandbox-reply`, and `chatbot-web-reply` (all three bundle the shared engine file) and verified end-to-end via the website widget: a 24x12x12in 6mm tank with 2 holes + 1 divider quoted Glass ₱936 + Holes ₱300 + Divider ₱140.40 = ₱1,376.40, matching the Order Now calculator exactly.
- Discovered the Hole/Divider inputs were missing from a THIRD place: the internal staff "Aquarium Calculator" portal page (`docs/aquarium-calculator.html`, embedding `docs/WebAquariumCalculator/index.html` - a separate, diverged copy of the calculator UI from Order Now's, though it shares the same underlying `custom-aquarium-calculator.js` pricing engine). Added the same Hole/Divider qty inputs there, wired into its `buildPayload`/pricing-rows-fetch/event-handler-list, and split "Aquarium Price" in its itemized summary into separate Aquarium/Hole/Divider price lines (matching how Stand is already broken out) instead of silently folding the new charges into one lump sum. Verified the breakdown sums exactly to the total.
- Resumed and finished the paused Telegram group-chat feature: added `supabase/functions/telegram-alice-webhook/index.ts`, an inbound webhook that lets the sales team talk to Alice right inside the existing staff Telegram group (or DM her 1:1). Reuses the real `buildSystemPrompt`/`runChatbotTurn` engine (same persona/knowledge as Messenger and the website widget) but always runs in `simulate: true` mode, so nothing said to her there creates a real order/escalation/CRM save. In a group she only replies when @mentioned or replied to (never to ordinary staff chatter); in a private DM she always replies. History is stored separately in `TelegramAliceMessages` ([supabase_telegram_alice_messages.sql](sql/supabase_telegram_alice_messages.sql)), not the customer-facing chatbot tables. Deployed the function and set a new `TELEGRAM_WEBHOOK_SECRET` function secret so the endpoint rejects spoofed requests.
