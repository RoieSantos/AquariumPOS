# Build outputs
bin/
obj/
publish/
build_temp/
installer/publish/

# Visual Studio / user-specific
.vs/
*.user
*.suo

# Logs
*.log

# Driver app (driver-app/) - Node/Capacitor build output. android/ is checked in (see
# driver-app/SETUP.md) once generated by `npx cap add android`, but its own Gradle build
# artifacts are still ignored.
driver-app/node_modules/
driver-app/www/
driver-app/android/app/build/
driver-app/android/build/
driver-app/android/.gradle/
driver-app/android/local.properties

