{
  "name": "rspetstop-driver-app",
  "version": "1.0.0",
  "private": true,
  "description": "Android app for RS Pet Stop delivery drivers - shares live GPS location with the web portal's Delivery page while a delivery route is active, including while backgrounded (e.g. navigating in Waze). See SETUP.md.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "sync": "npm run build && npx cap sync android"
  },
  "dependencies": {
    "@capacitor/android": "^7.0.0",
    "@capacitor/core": "^7.0.0",
    "@supabase/supabase-js": "^2.116.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0",
    "vite": "^5.4.0"
  }
}
